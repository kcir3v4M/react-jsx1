import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

const rootNode = document.getElementById('app');
ReactDOM.render(<App name='First React JSX usage' />, rootNode);
